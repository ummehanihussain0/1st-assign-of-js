// Declare a variable called 'age' and assign your age to it
let age = 20;

// Show the age in an alert box
alert("My age is " + age);

// Declare a variable called 'numVisits' and initialize it to 0
let numVisits = 13;

// Increment the number of visits by 1
numVisits++;

// Show the number of visits on the page
alert("You have visited this site " + numVisits + " times.");

// Declare a variable called 'birthYear' and assign your birth year to it
var show = "<h4>Birth Year :</h4>";
document.write(show);
let birthYear = 2002;
document.write("My birth year is " + birthYear + ".<br>");

// Display the data type of the variable in a message on the web page
var type = "<h4>Data type of variable :</h4>";
document.write(type);
document.write("Data type of my declaration is " + typeof(birthYear) + ".<br>");

// Declare variables to store visitor's name, product title, and quantity
let visitorName = "Ummehani Hussain";
let productTitle = "T-shirt";
let quantity = 5;

// Display a message showing the visitor's order details in the browser
var visit = "<h4>Online Clothing Store :</h4>";
document.write(visit);
document.write(visitorName + " ordered " + quantity + " " + productTitle + "(s) on XYZ Clothing store." +"\n");
